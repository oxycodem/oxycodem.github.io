import csv
import math

with open('schedule.csv', encoding='utf-8') as f:
    reader = csv.DictReader(f)
    for row in reader:
        h = int(row['hour'])
        m = int(row['minute'])
        tm = h * 60 + m
        dist = tm 
        print(row['station'], dist, sep='\t')